{
  "compilerOptions": {
    // Diretórios
    "rootDir": "./src",
    "outDir": "./out",

    // Módulos e resolução
    "module": "esnext",
    "target": "esnext",
    "moduleResolution": "node",   // 👈 garante que @prisma/client seja resolvido do node_modules
    "baseUrl": ".",               // 👈 resolve caminhos relativos a partir da raiz do projeto
    "paths": {
      "*": ["node_modules/*"]     // 👈 fallback pra pacotes externos
    },

    // Tipos e libs
    "types": ["node"],            // 👈 inclui definições de tipos do Node
    "lib": ["esnext"],

    // Outras configs
    "sourceMap": true,
    "declaration": true,
    "declarationMap": true,

    // Segurança e boas práticas
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": true,

    // Modo JSX (caso use React)
    "jsx": "react-jsx",

    // Otimizações
    "skipLibCheck": true,
    "isolatedModules": true
  },
  "include": ["src"],             // 👈 garante que só a pasta src entra na build
  "exclude": ["node_modules", "out"]
}

